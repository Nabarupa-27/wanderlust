<% if (success && success.length) { %>
  <div class="alert alert-success flash-box">
    <strong>Success!</strong> <%= success %>
  </div>
<% } %>

<% if (error && error.length) { %>
  <div class="alert alert-danger flash-box">
    <strong>Error!</strong> <%= error %>
  </div>
<% } %>
